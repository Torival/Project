/*#ifndef __VECTOR__HH__
#define __VECTOR__HH__

template <class T>

class Vector{
    public:
        Vector();
        ~Vector();
        T insert(int __position, T _val); 
        int size();
        void push_back(T _val); 
    private:
        T* _data;
        int _count; 
};

#endif */
